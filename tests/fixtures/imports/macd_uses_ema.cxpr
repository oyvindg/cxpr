name macd_uses_ema

use ema

in source

$period = 9

state {
    initialized = 0
    signal = 0
}

next_signal = if(initialized > 0, ema.ema_step(signal, source, $period), source)

initialized := 1
out signal := next_signal
